{
  "name": "poneglyph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "pull": "node scripts/pull-register.mjs",
    "paras": "node scripts/run-paras.mjs",
    "decide": "node scripts/decide.mjs",
    "attach": "node scripts/attach.mjs",
    "dev": "next dev",
    "build": "next build",
    "preview": "npx serve out -l 3000",
    "deploy": "next build && npx wrangler deploy",
    "deploy:preview": "next build && npx wrangler versions upload"
  },
  "dependencies": {
    "embla-carousel-react": "^8.6.0",
    "next": "^16.3.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5",
    "wrangler": "^4.120.0"
  }
}
