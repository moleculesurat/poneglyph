import type { ScraperCatch } from "@/lib/schema";

/* Watchtower — what the scraper caught on sebi.gov.in. Empty until the live
   watchtower runs and files a catch. */

export const catches: ScraperCatch[] = [];
